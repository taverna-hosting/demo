import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#D32F2F',
    },
    secondary: {
      main: '#FFC107',
    },
  },
  typography: {
    // Add any custom typography settings here
  },
});

export default theme;
